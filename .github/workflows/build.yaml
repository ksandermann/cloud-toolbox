name: GitHub Actions Demo
run-name: ${{ github.actor }} is BUILDING 🚀
on:
  push:
    branches:
      - master
      - '2022-11-19'
jobs:
  Create-Branch:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: build images
        run: bash ./build.sh

